[ entry point ]
dragon.main()() = {
    [ build & run compiler ]
    dragon.set("./source/tests/pirate.dragon")(user_code)
    dragon.pack.null()(input !dragon.buffer)
    dragon.just_run.one_file(user_code, input)(result, error)
}
